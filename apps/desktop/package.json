{
  "name": "desktop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"Desktop dev uses Next dev server at http://localhost:3000\"",
    "build": "echo \"Desktop build expects ./dist populated by root build:desktop:export\"",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.7.1"
  }
}
